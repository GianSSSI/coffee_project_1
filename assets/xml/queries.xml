<queries>
  <query id="insert_coffee">
    INSERT INTO coffee (
      coffeeId,
      title,
      description,
      image,
      ingredients,
      label
    ) VALUES (?, ?, ?, ?, ?, ?);
  </query>

  <query id="get_all_coffee">
    SELECT * FROM coffee;
  </query>

<query id="get_coffee_by_id">
  SELECT * FROM coffee WHERE coffeeId = ?;
</query>

</queries>
