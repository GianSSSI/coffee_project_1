<queries>
  <query id="insert_coffee">
    INSERT OR REPLACE INTO coffee (
      coffeeId,
      title,
      description,
      image,
      ingredients,
      label
    ) VALUES (?, ?, ?, ?, ?, ?);
  </query>

  <query id="get_all_coffee">
    SELECT * FROM coffee;
  </query>

<query id="get_coffee_by_id">
  SELECT * FROM coffee WHERE coffeeId = ?;
</query>

<query id ="create_coffee_table">
    CREATE TABLE coffee (
    coffeeId TEXT PRIMARY KEY,
    title TEXT,
    description TEXT,
    image TEXT,
    ingredients TEXT,
    label TEXT
  )
</query>

</queries>
